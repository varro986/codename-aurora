namespace CodenameAurora.Core;

/// <summary>Placeholder — remove when the first real domain type is added.</summary>
public static class CorePlaceholder
{
    /// <summary>Returns true. Used to verify the CI pipeline end-to-end.</summary>
    public static bool PipelineReady() => true;
}
