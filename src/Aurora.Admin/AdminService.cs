namespace Aurora.Admin;

// Placeholder — keeps the Aurora.Admin assembly loadable by NetArchTest.
// Implementation is driven by user stories (IAppSettings, auto-update).
public sealed class AdminService
{
}
